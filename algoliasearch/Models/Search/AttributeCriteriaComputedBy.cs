//
// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
//
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using Algolia.Search.Serializer;

namespace Algolia.Search.Models.Search;

/// <summary>
/// Strategy for computing the Attribute ranking criterion. This mainly affects multi-word queries with matches in more than one attribute.  The `ranking` setting decides whether `best` takes effect. When Attribute comes after Proximity, which is the default order, the engine always uses the `minProximity` strategy and ignores `best`. To select `best`, move Attribute before Proximity in `ranking`. The `sum` strategy applies in both orders.  - `minProximity`.   Pick the best matching attribute from the attributes that form the best proximity score.   On an ordered attribute, the match position breaks ties. - `best`.   Pick the best matching attribute from all attributes that match any query word.   On an ordered attribute, the match position breaks ties. - `sum`.   Add up a score for every query word instead of picking one attribute.   Each word's score comes from the attribute it matched, and from its position in that attribute when the attribute is ordered.   A query word that matches nothing adds a large penalty.   A record with a lower total ranks higher.   A record therefore cannot rank high only because one word of a multi-word query matched a top attribute.   Use `sum` with short, relevant attributes, and set long-text attributes to unordered.
/// </summary>
/// <value>Strategy for computing the Attribute ranking criterion. This mainly affects multi-word queries with matches in more than one attribute.  The `ranking` setting decides whether `best` takes effect. When Attribute comes after Proximity, which is the default order, the engine always uses the `minProximity` strategy and ignores `best`. To select `best`, move Attribute before Proximity in `ranking`. The `sum` strategy applies in both orders.  - `minProximity`.   Pick the best matching attribute from the attributes that form the best proximity score.   On an ordered attribute, the match position breaks ties. - `best`.   Pick the best matching attribute from all attributes that match any query word.   On an ordered attribute, the match position breaks ties. - `sum`.   Add up a score for every query word instead of picking one attribute.   Each word's score comes from the attribute it matched, and from its position in that attribute when the attribute is ordered.   A query word that matches nothing adds a large penalty.   A record with a lower total ranks higher.   A record therefore cannot rank high only because one word of a multi-word query matched a top attribute.   Use `sum` with short, relevant attributes, and set long-text attributes to unordered. </value>
[JsonConverter(typeof(Serializer.JsonStringEnumConverter<AttributeCriteriaComputedBy>))]
public enum AttributeCriteriaComputedBy
{
  /// <summary>
  /// Enum MinProximity for value: minProximity
  /// </summary>
  [JsonPropertyName("minProximity")]
  MinProximity = 1,

  /// <summary>
  /// Enum Best for value: best
  /// </summary>
  [JsonPropertyName("best")]
  Best = 2,

  /// <summary>
  /// Enum Sum for value: sum
  /// </summary>
  [JsonPropertyName("sum")]
  Sum = 3,
}
